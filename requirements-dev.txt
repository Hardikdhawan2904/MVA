# Test dependencies, only needed for MVA-use-case-latest-one's test suite.
# Install with: pip install -r requirements.txt -r requirements-dev.txt

pytest
pytest-asyncio
pytest-cov
factory-boy
freezegun
