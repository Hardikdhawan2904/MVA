# Postgres for this project now runs from the shared server used by all agents:
#   C:\Users\dhawa\mva\Shared-Postgres\docker-compose.yml  (docker compose up -d)
#
# It hosts the single `mva_pipeline` database on port 5433, with this project's
# tables isolated in the `agent2` schema (same credentials this project always
# used, mva_user/mva_password). Do not `docker compose up` in this directory
# anymore for Postgres.
#
# (The `api` service previously defined here was never actually used — the
# app has always been run locally via venv + uvicorn, not containerized.)
